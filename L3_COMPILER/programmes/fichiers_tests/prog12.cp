CPYRR

int var x;

string[6] var chainedetoto; 

typedef char tabDeChar[-5,4;5,9;-10,9];

typedef struct declarType{
    typeperso x;
    typeperso2 y;
};

//ceci est un superbe commentaire permettant de commenter 
fonction bigBigFonction(int x) ret int{ //et en voila un deuxieme
    typedef char tabDeChar[-5,4;5,9;-10,9]; 

    /*ceci est un
    bloc de commentaire
    permettant un 
    super commentaire
    */

    typedef struct declarType{
        typeperso x;
        typeperso2 y;
    };

    int var x;
    string[6] var chainedetoto; 

    procedure p1(){
        typedef struct toto{
            int toto;
            string[10] tata;
        };

        DEBUT
        toto.toto.tata[4]=2;
        VIDE;
        FIN
    };

     fonction fonctionInFonc(string[4] toto) ret string[4]{
        DEBUT
        POUR(i=2; i>=0; i=i+i) FAIRE{
            DEBUT
            
            POUR(toto[titi.tata[2*5]] = teteatoto(); tata<=titi.tata[2]; toto=tata+titi) FAIRE{
                DEBUT
                VIDE;
                FIN
            };

            FIN
        };
        FIN
    };

    DEBUT
    
    TANT QUE (lateteatoto == 0+0)FAIRE{
        DEBUT
        SI (toto==2)ALORS{
            DEBUT
            ret 4;
            FIN
        };
        FIN
    };

    VariableTestEntiere = 3;
    VariableTestChar = 'Z';
    VariableTestFloat = 1298.031531511210053;
    VariableTestBool = FALSE;
    VariableTestBool = TRUE;
    VariableTestVariable = vartest;
    VariableTestChaine = "ok";

    x = toto[tutu.bla[y*f(x+g)/2]].tutu;
    f[g[t[f(y*tutu)*g(toto/tata)].blablia].truc[f(2)*f(2)].blabli].x[g[f(4)*f(5)].tutu].tata = 2;

    SI (x+toto == tata) ALORS{
        DEBUT
        SI (toto==42+53*teteatoto OU machin>=bidule*toto+45.256) ALORS{
            DEBUT
            SI ((a==b ET x>c) OU (NON z>=x OU TRUE)) ALORS{
                DEBUT
                SI (((a+3*2)==(x*5+(3/2)- 5) OU (a<=z ET z>=a) OU NON a>x) OU TRUE) ALORS{
                    DEBUT VIDE; FIN
                };
                FIN
            }
            SINON{
                DEBUT 
                    x=appelFonction(appelfctTITI(43,FALSE), y, toto, TRUE, 45.543, (x+y*52));
                    appelTOTO((x==2 OU y==1), (NON(tutu+toto*4)-(tata*titi)/5-bidule*truc==1));
                    varTOTOTITITATA=oktotobidule((x*25<toto ET bidule/truc==2), varToto, 'c',((tutu+toto*4)-(tata*titi)/5-bidule*truc));
                FIN
            };

            ret x+toto*biduletruc;
            FIN
        };
        FIN
    }
    SINON{
        DEBUT 
        SI ((a==b ET x>c) OU (NON z>=x OU TRUE)) ALORS{
            DEBUT
            SI (((a+3*2)==(x*5+(3/2)- 5) OU (a<=z ET z>=a) OU NON a>x) OU TRUE) ALORS{
                DEBUT a=bidule; ret TRUE; FIN
                
            };
            FIN
        }
        SINON{
            DEBUT a=truc;
            ret FALSE;
            FIN
        };
        FIN
    };
    
    appel_de_la_tete_a_toto();
    appel_de_la_tete_a_titi("arg1",'c', 2, varToto, 2.999, TRUE, (x==2 OU y==x- 2),
                            appel_de_la_tete_a_tata("arg1",'c', 2, varToto, 2.999, TRUE, (x==2 OU y==x- 2)),
                            45+34+(toto*varTata));


    ret x;

    FIN
};

procedure testProcedure(int x, char toto, float testFloat, bool testBool){
    typedef char tabDeChar[-5,4;5,9;-10,9];

    typedef struct declarType{
        typeperso x;
        typeperso2 y;
    };

    int var x;

    fonction fonctionInProc(string[4] toto) ret string[4]{
        DEBUT
        POUR(i=2; i>=0; i=i+i) FAIRE{
            DEBUT
            
            POUR(toto[titi.tata[2*5]] = teteatoto(); tata<=titi.tata[2]; toto=tata+titi) FAIRE{
                DEBUT
                VIDE;
                FIN
            };

            FIN
        };
        FIN
    };

    DEBUT

    VariableTestEntiere = 3;
    VariableTestChar = 'Z';
    VariableTestFloat = 1298.031531511210053;
    VariableTestBool = FALSE;
    VariableTestBool = TRUE;
    VariableTestVariable = vartest;

    x=appelFonction(appelfctTITI(43,FALSE), y, toto, TRUE, 45.543, (x+y*52));
    appelTOTO((x==2 OU y==1), (NON(tutu+toto*4)-(tata*titi)/5-bidule*truc==1));
    varTOTOTITITATA=oktotobidule((x*25<toto ET bidule/truc==2), varToto, 'c',((tutu+toto*4)-(tata*titi)/5-bidule*truc));
    
    TANT QUE (lateteatoto == 0+0)FAIRE{
        DEBUT
        proceduredeToTo(avecUnArgument1, unArgument2);
        
        SI (toto==2)ALORS{
            DEBUT
            ret 4;
            FIN
        };
        ret lateteatata; //voila un super commentaire sur une ligne
        FIN
    };

    FIN
};


DEBUT 

VariableTestEntiere = 3;
VariableTestChar = 'Z';
VariableTestFloat = 1298.031531511210053;
VariableTestBool = FALSE;
VariableTestBool = TRUE;
VariableTestVariable = vartest;

SI (x == y) ALORS{
    DEBUT
    x=6;
    VariableTestChar = 'Z';
    VariableTestFloat = 1298.031531511210053;
    FIN
}
SINON{
    DEBUT 
    x=4;
    SI (varToto < varTata) ALORS{
        DEBUT
        x42x = 5;
        FIN
    }
    SINON{
        DEBUT
        ytoto = 10;
        FIN
    };
    FIN
};

SI ((a==b ET x>c) OU (NON z>=x OU TRUE)) ALORS{
    DEBUT
    SI (((a+3*2)==(x*5+(3/2)- 5) OU (a<=z ET z>=a) OU NON a>x) OU TRUE) ALORS{
        DEBUT 
            x=appelFonction(appelfctTITI(43,FALSE), y, toto, TRUE, 45.543, (x+y*52));
            appelTOTO((x==2 OU y==1), (NON(tutu+toto*4)-(tata*titi)/5-bidule*truc==1));
            varTOTOTITITATA=oktotobidule((x*25<toto ET bidule/truc==2), varToto, 'c',((tutu+toto*4)-(tata*titi)/5-bidule*truc));
        FIN
    };
    FIN
}
SINON{
    DEBUT a=c;FIN
};

SI (5+3*a/(toto+35.900092*(25.25/2))/2 <= 42.42*2- 2 OU (x+y-toto*nimportevar)==0) ALORS{
    DEBUT
        x=appelFonction(appelfctTITI(43,FALSE), y, toto, TRUE, 45.543, (x+y*52));
        appelTOTO((x==2 OU y==1), (NON(tutu+toto*4)-(tata*titi)/5-bidule*truc==1));
        varTOTOTITITATA=oktotobidule((x*25<toto ET bidule/truc==2), varToto, 'c',((tutu+toto*4)-(tata*titi)/5-bidule*truc));

        TANT QUE (lateteatoto == 0+0)FAIRE{
            DEBUT
            SI (toto==2) ALORS{
                DEBUT
                ret 4;
                FIN
            };
            FIN
        };
    FIN
};

//salut ceci est un commentaire sur une seule ligne

ret 4;             //ceci également
ret TRUE;
ret FALSE;
ret 'c';
ret 45.3897489335435438481681194943186;
ret varToto;
ret (TRUE OU (x+2*y)<z);
ret (toto+toto*tete/toto);

appelFonction(x, y, toto, TRUE, 45.543, (x+y*52));
appelTOTO((x==2 OU y==1), (NON(tutu+toto*4)-(tata*titi)/5-bidule*truc==1));
oktotobidule((x*25<toto ET bidule/truc==2), varToto, 'c',((tutu+toto*4)-(tata*titi)/5-bidule*truc));
la_tete_a_toto();

TANT QUE (5+3*a/(toto+35.900092*(25.25/2))/2 <= 42.42*2- 2 OU (x+y-toto*nimportevar)==0) FAIRE{
    DEBUT
        SI (toto==42+53*teteatoto OU machin>=bidule*toto+45.256) ALORS{
            DEBUT
            SI ((a==b ET x>c) OU (NON z>=x OU TRUE)) ALORS{
                DEBUT
                SI (((a+3*2)==(x*5+(3/2)- 5) OU (a<=z ET z>=a) OU NON a>x) OU TRUE) ALORS{
                    DEBUT VIDE; FIN
                };
                FIN
            };
            
            TANT QUE (lateteatoto == 0+0)FAIRE{
                DEBUT
                SI (toto==2)ALORS{
                    DEBUT
                    TANT QUE (lateteatoto == 0+0)FAIRE{
                        DEBUT
                        
                        SI (toto==2)ALORS{
                            DEBUT
                            ret 4;
                            appelTeteAToto("sAlUt WoRlD!!!", appelTeteATiti("Bonjour a tous"));
                            x = "142~#[{#[:;f!ez;:;!)()''|fzae**]}]  12.3456         |zajf ;::!:!#{]@]|}d''dkz,d''''elk'f,ez'";
                            appelTeteAToto();
                            FIN
                        };

                        POUR(i=2; i>=0; i=i+i) FAIRE{
                            DEBUT
                            
                            POUR(toto[titi.tata[2*5]] = teteatoto(); tata<=titi.tata[2]; toto=tata+titi) FAIRE{
                                DEBUT
                                VIDE;
                                FIN
                            };

                            FIN
                        };
                        ret "ok";
                        FIN
                    };
                    FIN
                }; 
                //salut ceci est un commentaire sur une seule ligne
                FIN
            };

            ret x+toto*biduletruc;

            y.toto[2].f.t[4].toto[x[3+2*toto].tata[y[TRUE]]] = 2;
            f[x.y[a*b].z[toto[tata[(TRUE OU FALSE)].bli[blo["salut c'est bla"]]]]] = appel_de_la_tete_a_toto(1);
            x[f(3)*y,a[g*z(a)].ch1].toto.bli.bla[2].blo[tata[tutu+titi*4/2]]= 2;
            x[f(3)*y,a[g*z(a)].ch1,f(champ3)*tutu[2*1].toto]="salut";

            FIN 
        };
    FIN
};

x=teteatoto;

SI (f[g[t[f(y*tutu)*g(toto/tata)].blablia].truc[f(2)*f(2)].blabli].x[g[f(4)*f(5)].tutu].tata == toto[tutu.bla[y*f(x+g)/2]].tutu) ALORS{
    DEBUT
    superResultat = gagner;
    FIN
};

FAIRE{
    DEBUT
    trucbidulemachinchouette();
    FIN
}TANT QUE(x==1);

POUR(i=2; i>=0; i=i+i) FAIRE{
    DEBUT
    
    POUR(toto[titi.tata[2*5]] = teteatoto(); tata<=titi.tata[2]; toto=tata+titi) FAIRE{
        DEBUT
        VIDE;
        FIN
    };

    FIN
};

FIN

CPYRR